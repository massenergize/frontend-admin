// import { TextField, withStyles } from "@material-ui/core";

// // -------------------------------------------------------------------------------------
export const NULL = "------";
export const ALL = "All";
// export const Dropdown = ({ classes, onChange, value, data = [] }) => {
//   const handleOnchange = (e) => {
//     if (!onChange) return;
//     onChange(e.target.value);
//   };
//   return (
//     <Select
//       multiple
//       className={classes.dropdown}
//       onChange={async (e) => handleOnchange(e)}
//       value={value}
//     >
//       {/* <option className={classes.dropdownItem}>{NULL}</option>
//         <option className={classes.dropdownItem}>{ALL}</option> */}
//       {data.map((com, index) => {
//         return (
//           <MenuItem key={index}>
//             <FormControlLabel
//               key={com.id}
//               control={<Checkbox value={com.id} name={com.name} />}
//               label={com.name}
//             />
//           </MenuItem>
//         );
//       })}
//     </Select>
//   );
// };
